# Recipe photos

Placeholder directory (spec §5, §13.2). Drop the founder's actual recipe photos
here, named to match each recipe's `photoUrl` in `lib/seedData.ts`
(e.g. `sheet-pan-chicken.jpg`).

Until real photos exist, the recipe screen renders a `bg-tan` fallback with a
subtle radial shape — no broken-image icons. Photograph the six meals on an
iPhone in natural light, on real surfaces, no styling (founder-as-first-user).
